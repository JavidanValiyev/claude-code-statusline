# claude-code-statusline
Custom status line configuration for Claude Code CLI — shows git branch, model name, context usage %, session cost,   ▎ 5-hour rate limit with reset countdown, and weekly usage. Drop-in setup for ~/.claude/. Works on Windows (Git Bash) 
